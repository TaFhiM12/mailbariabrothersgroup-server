{
  "version": 2,
  "buildCommand": "npx prisma generate && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "crons": [
    {
      "path": "/api/v1/cron/monthly-payment-reminder",
      "schedule": "0 0 10 * *"
    }
  ]
}